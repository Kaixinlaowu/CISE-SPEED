export default function AnalysisChart({ data }) {
  return (
    <div className="bg-white shadow p-6 rounded-lg border">
      <h4 className="font-semibold mb-4">图表可视化</h4>
      <div className="p-10 border rounded-lg bg-gray-50 text-center text-gray-500">
        {/* 这里你可以集成 Chart.js / ECharts */}
        <p>这里将显示图表（需前端图表库）</p>
      </div>
    </div>
  );
}
